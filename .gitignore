# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# vite
/dist
/node_modules/.vite
/node_modules/.cache

# next.js (leftover from migration — safe to keep for one cycle)
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code local config (not part of the portfolio)
/.claude/

# local dev logs (Vite HMR noise, not source)
/dev.log

# local screenshots / review artifacts (not part of the portfolio)
/screenshots/
/review-*.png

# README section screenshots (kept only locally, not in the repo)
/project-images/